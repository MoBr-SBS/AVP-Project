api:
  listen: ":1984"
  token: "aB7dE9fG2hJ5kL8mN1pQ"

  tls_cert: "/home/bot/bot-server/code/certs/cert.pem"
  tls_key: "/home/bot/bot-server/code/certs/key.pem"
  tls_listen: ":1985"

streams:
  cam:
    #Kamera
    - exec:ffmpeg -f v4l2 -input_format mjpeg -video_size 1280x720 -i /dev/video0 -c:v libx264 -preset ultrafast -tune zerolatency -f h264 -
    #Mikrofon
    #- ffmpeg:device?audio=hw:0,0&channels=1
